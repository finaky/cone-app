import { HeaderHero } from "./HeaderHero";
import { HeaderNavbar } from "./HeaderNavbar";
export const Header = () => {
  return (
    <header>
      <HeaderNavbar />
      <HeaderHero />
    </header>
  );
};
